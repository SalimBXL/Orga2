<% if current_user.present? %>
    <div class="container">
        <div class="mt-2 p-5 rounded-3 jumbotron">
            <h1>ORGA</h1> 
            <p>Welcome <%= current_user.firstname %></p> 
        </div>
    </div>

    <%= render "main/dashboard/dashboard" %>
<% end %>